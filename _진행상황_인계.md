# 진행상황 인계 — 신차/중고 견적기 통합 + ERP4 임베드

> 작성 2026-07-23. 재설치 후 이 문서부터 읽고 이어서 진행.
> 관련 레포 3개가 얽혀 있음. 각 레포 루트에도 같은 성격의 `_진행상황_인계.md` 있음.

## 큰 그림 (3개 레포)
- **welrixtable** (이 repo, `D:\dev\welrixtable`) — 신차 장기렌터카 견적기. Vue3+Vite+Firebase. 배포 `welrixtable.vercel.app`.
- **sonogong-estimator** (`D:\dev\sonogong-estimator`) — 중고 렌트/구독 견적기. Vue3+Vite+Firebase. 배포 `sonogong-estimator.vercel.app`.
- **freepasserp4** (`D:\dev\freepasserp4`) — Next15/React/TS ERP. 두 견적기를 **페이지 공유(iframe)로 임베드**.

**목표**: 두 견적기 = 같은 디자인시스템(토큰·크기·UX 동일) 위에 **CI색·차선택·원가 산출만 다르게**. erp4는 각 견적기를 수정 없이 iframe 임베드. (구조통합은 UI/계산이 아니라 "페이지 공유" 방식으로 결정됨)

## ⚠️ 가장 중요 — erp4가 임베드하는 welrix는 이 repo가 아님
- erp4 `/welrix` 는 **`welrixmobility.netlify.app`** (웰릭스가 직접 제공·관리하는 **별도 배포본**)을 그대로 임베드. 액센트 **블루**. 우리가 수정하지 않음(웰릭스 제공본).
- 이 repo(welrixtable)의 작업(엑셀 v6.1 계산·CI 레드·임베드 브리지)은 `welrixtable.vercel.app`에 배포돼 있으나 **erp4 임베드엔 미사용**. 소스/참고용.
- erp4 `/sonogong` 은 우리 레포(`sonogong-estimator.vercel.app`)를 임베드 — 이쪽은 임베드 브리지 있음.

## 이 repo(welrixtable)에서 한 작업 — `main` 브랜치, 배포 완료
1. **엑셀 v6.1 계산 동기화** (`웰릭스모빌리티 신차 견적(20260615)_v6.1_배포용.xlsx` 기준)
   - 개별소비세 3.5%→5%, 제네시스 잔가 −20%→−18%·인수가율 +25%→+23%, 보증금(저신용 CEILING 50만/그외 ROUND 10만, 상한 1000만), H34 기간가산 기준 H27→H33 버그수정
   - 계산엔진 SSOT = `src/lib/calc.js` (index/mobile/home 전부 이걸 import)
   - 정책 config = `public/data/company-config/welrix.json` (런타임) + `calc.js` DEFAULT_CFG (폴백). **검증: `node scripts/check-sync.mjs`** (3소스 일치 + 엑셀 회귀 + 차량DB)
2. **규격·효율화** — 데드코드 −170줄(quote.js), 미사용 심볼, guessColor dedup, config 죽은키
3. **SSOT 정리** — src config 유령사본 제거, check-sync 2소스화
4. **모바일 버그 2건** — 발송 필터 미반영(SendSheet send:[...quoteState.send]), `?q` 견적링크 빈화면(index 리다이렉트에서 ?q/?admin 제외)
5. **CI 레드 통일** — 견적기(index/mobile) --brand 네이비→#e1141e (랜딩은 이미 레드였음)
6. **토큰 통일** — 정본 `--fs-*/--r-*/--sh-*/--sp-*` 별칭 추가(손오공과 동일 규격). `src/styles/tokens.css` = 공유토큰 SSOT 규격서
7. **임베드 브리지** — `src/lib/embed-bridge.js` (아래 프로토콜). `vercel.json` frame-ancestors `*.vercel.app`

## 임베드 브리지 프로토콜 (welrix·sonogong 공통 — `src/lib/embed-bridge.js`)
- child→host(erp4): `{ns:'welrix'|'sonogong', type:'ready'|'resize'|'quote', payload}`
- host→child: `{ns:'welrix:host', type:'prefill', payload}` + URL 파라미터 프리필(`?embed=1&parentOrigin=<erp4>&custName=..&dealCode=..`)
- 결과 quote payload: `{id,url,dealCode,summary}` → erp4가 딜/계약 연결
- `isEmbedded()=false`면 완전 no-op(스탠드얼론 무영향). 신뢰 origin 화이트리스트 `ALLOWED_PARENT_ORIGINS`

## 남은 것 (이어서 할 후보)
- **진짜 컴포넌트 공유** (shared ui-kit / monorepo `packages/estimator-core`+`ui-kit` + `apps/*`) — 지금은 "같은 토큰·다른 파일". 재무엔진(PMT/IRR·원가조립)은 두 앱 구조 거의 동일(손오공 `resolveResidualRate`가 per-차량 rates OR group 폴백 → 정본 리졸버 후보). erp4(React)엔 UI 공유 불가·**헤드리스 코어(TS)만 공유** 가능(하지만 지금은 페이지공유라 불필요).
- UI/UX 리뷰 남은 HIGH — 보증금 clamp 30% vs 100% 불일치, 표준견적 프리셋 15곳 복붙 SSOT화, CustomDropdown 접근성, RightPanel god-component 분리 등. (리뷰 아티팩트는 claude.ai/code/artifact/e16258a5-69c5-4fca-a9a0-a32265fd095b)
- welrixmobility(웰릭스 제공본)가 v6.1 계산인지 여부는 웰릭스 책임 — 우리 관여 안 함

## 브랜치/커밋
- 작업 브랜치 `v6.1-sync-and-cleanup` → `main` 병합·푸시 완료(Vercel 배포됨). 최신 main에 위 전부 포함.
- 임시 검증 스크립트는 세션 scratchpad에 있었음(재설치 시 소멸). **durable 검증 = `node scripts/check-sync.mjs`**.
