# 조건표 이동

- 사용자 최종 지시: 하단 `다음` 버튼 바로 옆에 `조건표`를 둔다. 뒤로가기가 아니라 항상 `https://welrix-rent.web.app/` 목록으로 이동한다.
- 이전 차량/브라우저 이력/외부 redirect 값을 사용하지 않는다.
- 프로모션에서 견적기 진입은 mobile.html?force=mobile로 연결하며 기존 b/m/t는 유지한다.
- build/check:quote-contract 및 320/390/1280px 양방향 이동 검증.
- 운영 배포: `fb0f44f`, Vercel `dpl_9AshhzN4tAFLTfUkiSP25ETAHtns` READY. `welrixtable.vercel.app/mobile.html`에서 320/390/1280px 모두 하단 `조건표 | 다음`, 상단 중복 없음, 가로 넘침 없음 확인.
